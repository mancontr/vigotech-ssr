// Can't use import on browser without Webpack.
// We just add them as <script>s instead.
// import React from 'react'
// import ReactDOM from 'react-dom'

ReactDOM.render(
  <h1>Hola, mundo!</h1>,
  document.getElementById('root')
)
